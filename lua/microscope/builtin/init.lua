return {
  actions = require("microscope.builtin.actions"),
  layouts = require("microscope.builtin.layouts"),
  lenses = require("microscope.builtin.lenses"),
  parsers = require("microscope.builtin.parsers"),
}
