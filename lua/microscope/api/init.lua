return {
  command = require("microscope.api.command"),
  display = require("microscope.api.display"),
  error = require("microscope.api.error"),
  highlight = require("microscope.api.highlight"),
  lens = require("microscope.api.lens"),
  scope = require("microscope.api.scope"),
}
